// Deploy test marker 2026-06-10 — no-op, safe to remove.
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  devIndicators: false,
};

export default nextConfig;
