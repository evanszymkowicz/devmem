# Complete Action

> Note: the user handles all git operations (commit, merge, push, branch deletion). Do NOT run them.

1. Reset current-feature.md:
   - Change H1 back to `# Current Feature`
   - Clear Goals and Notes sections (keep placeholder comments)
   - Add feature summary to the END of History
2. Tell the user the feature is complete and remind them to commit, merge, and push (suggest a conventional commit message for the reset, e.g. `chore: reset current-feature.md after completing [feature]`)
